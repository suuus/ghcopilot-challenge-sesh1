// Implement a function that finds the longest palindrome substring
// A palindrome reads the same forwards and backwards
// Example: "racecar" is a palindrome

export function findLongestPalindrome(str: string): string {
    // TODO: Implement this function
    // Tips:
    // - Consider edge cases (empty string, single character)
    // - Think about expanding around centers
    // - Handle even and odd length palindromes
    return '';
}

// Optional: Helper function to check if a string is a palindrome
// TODO: Implement isPalindrome helper function